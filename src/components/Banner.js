const Banner = ()=>{
    const banner = `
      <h1>Welcome to parcel configuration</h1>
      <p>This configuration is for simple bundling workflow for frontend</p>
    `
    return banner
}

export default Banner